PORT=5001
NODE_ENV=development

# MySQL Configuration
# If MySQL is not reachable, the app automatically uses SQLite (podmob.sqlite) as a zero-config fallback.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=podmob
